<% if response.status == 200 %>
  $('.subscription').html('<%= link_to('subscribe', question_subscriptions_path(@question), method: :post, remote: true) %>');
<% end %>

$('.flash').html('<%= bootstrap_flash %>');
<% flash.discard %>
